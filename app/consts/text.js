import { StyleSheet } from "react-native";

export const textStyles = StyleSheet.create({
  headerText: {
    fontSize: 18,
    fontWeight: "bold",
  },
  italicText: {
    fontStyle: "italic",
  },
});
